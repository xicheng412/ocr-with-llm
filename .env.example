# OCR Tool Environment Configuration

# Required: OpenAI-compatible API Key
# Get your API key from your AI provider (OpenAI, Gemini, etc.)
OPENAI_API_KEY=your_api_key_here

# Optional: Custom base URL for OpenAI-compatible API (for proxy or different providers)
# Leave empty to use default OpenAI API endpoint
# OPENAI_BASE_URL=https://your-proxy-url.com/v1

# Optional: Default model to use
# Available models: gpt-4o-mini, gpt-4o, gemini-1.5-flash (if using Gemini via OpenAI format), etc.
# OPENAI_MODEL=gpt-4o-mini